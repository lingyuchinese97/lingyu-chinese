import type { Messages } from "../vi";

export const api: Messages["api"] = {
  jsonOnly: "Only JSON is accepted (Content-Type: application/json).",
  docsTitle: "API documentation",
  docsIntro: "Sign in to LingYu (or call “Sign in” under Tài khoản) then press “Try it out” to call each route.",
  docsLoading: "Loading API documentation…",
  docsLoadFailed: "Couldn't load the API documentation. Please reload the page.",
};
